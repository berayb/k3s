apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: name-virtual-host-ingress
spec:
  rules:
  - host: api-flask.local
    http:
      paths:
      - path: /
        backend:
          serviceName: k3s-flask
          servicePort: 80
  # - host: service-b.example.com
  #   http:
  #     paths:
  #     - backend:
  #         serviceName: service-a
  #         servicePort: 80